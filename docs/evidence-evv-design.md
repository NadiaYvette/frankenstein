# Plotkin-Style Evidence-Vector Dispatch (D-series)

## Goal

Replace Frankenstein's compile-time inline handler-binding (the default
`evidencePassGlobal` in `Frankenstein.Core.Evidence`) with runtime
evidence-vector dispatch, mirroring Plotkin/Pretnar's evidence-passing
semantics for algebraic effects. Cross-function effect dispatch becomes
principled rather than relying on global name lookup.

Gated behind `--evidence=plotkin` (default is `inline`).

## Runtime ABI (D1)

`runtime/kk_runtime.{c,h}` adds:

```c
int64_t kk_evv_extend(int64_t parent, int64_t eff_id, int64_t op_table);
int64_t kk_evv_lookup(int64_t evv, int64_t eff_id);
int64_t kk_optab_create(int64_t nops);
int64_t kk_optab_set(int64_t tab, int64_t op_idx, int64_t closure);
int64_t kk_optab_get(int64_t tab, int64_t op_idx);
```

An `evv` is an `KK_EVV2_TAG`-tagged `kk_alloc_con` whose payload is a
sequence of `(eff_id, op_table)` pairs. Top-of-stack is the highest
slot; `kk_evv_lookup` linear-searches top-down so inner handlers
shadow outer.

An `op_table` is a `KK_OPTAB_TAG`-tagged `kk_alloc_con` whose fields
are closures (one per operation).

Effect ids are deterministic djb2 hashes of the effect name (computed
by both compiler and runtime, so they agree without an intern table).

`kk_drop` treats `KK_EVV2_TAG` as "drop only odd-indexed fields"
(op_tables), since even slots are integers. `KK_OPTAB_TAG` drops every
field. Both are acyclic from the cycle collector's perspective.

`runtime/test_evv_plotkin.c` covers single lookup, nested extend
shadowing, and unrelated lookup — all pass.

## Compile-time Pass (D2)

`Frankenstein.Core.EvidenceEvv.evidencePassEvv :: Program -> Program`
performs a Core→Core rewrite:

```
EHandle effRow handler body
  ==>
  let op_table = kk_optab_create(n_ops)
      _evv_set = kk_optab_set(op_table, 0, handler)
      ...
      new_evv  = kk_evv_extend(<current_evv>, EFF_ID, op_table)
  in body[<current_evv> -> new_evv]

EPerform eff.op args
  ==>
  let tab  = kk_evv_lookup(<current_evv>, EFF_ID)
      clos = kk_optab_get(tab, OP_IDX)
  in clos(args)
```

The pass is parameterised over the in-scope evv `Name`. Initially the
root evv comes from the function's first formal (D3).

## Cross-Function Threading (D3)

Every top-level definition in plotkin mode gets `evv_p` prepended as
its first formal:

```
foo : (any) -> int -> int            -- evv as first arg
foo = fn(evv_p, x) ...

bar : (any) -> int
bar = fn(evv_p) foo(evv_p, 0)        -- caller threads evv to callee
```

`EApp (EVar fn) args` is rewritten to `EApp (EVar fn) (EVar evv_p : args)`
whenever `fn` refers to a top-level definition in the linked program.
Runtime helpers (`kk_*`) are recognised by absence from the top-name
set and pass through unchanged.

The MLIR emitter detects `main`'s arity from its declared type. When
`main` has 1 arg (the evv after the plotkin pass), the C entry-point
wrapper emits `%c0_evv = arith.constant 0 : i64` and passes it to
`Module__frankenstein_main(%c0_evv)`. Inline mode is unaffected:
0-arg main stays 0-arg.

## Parity Report (D4)

| Example | Inline mode | Plotkin mode |
|---|---|---|
| `effect_ask` (single-fn, abort-style) | garbage* | 84 ✓ |
| `effect_state` (single-fn, abort-style) | garbage* | 100 ✓ |
| `effect_ask_xfn` (cross-fn, abort-style) | n/a | 99 ✓ |
| `effect_nondet` (multi-shot) | 30 ✓ | segfault** |
| `--demo` (factorial, no effects) | 3628800 ✓ | n/a |

\* Pre-existing inline-mode bug: 1-arg handler called with 0-arg
perform produces a PAP closure pointer instead of invoking the
handler. Plotkin's closure-ABI-via-fn-pointer dispatch sidesteps this.

\*\* Multi-shot under plotkin is the open D5+ work. The CPS-converted
continuation closures generated by `cpsTopLevel` (B2-full) don't carry
`evv`. When a reified continuation is invoked later, it has no evv to
look up the handler. Two paths forward:

1. Make CPS continuations carry the captured evv as an extra closure
   field. The cps-converter would need to know which evv is in scope
   at each `EPerform` site.
2. Make `kk_current_evv` a thread-local that CPS continuations
   save/restore (dynamic scoping, sacrifices some compositionality).

## Known Gaps / Open Work

- **D5: multi-shot via evv-in-continuation** — done; nondet → 30 ✓
- **D6: K-verify the evv lowering** — done; 15 EVV claims #Top ✓
- **Tail-resumptive evv** isn't actually exercised yet: the
  `effect_ask`/`effect_state` cases happen to work because the handler
  ignores its formal parameter. A handler that uses both its arg and
  calls resume hasn't been tested.
- **Bootstrap parity (DB1-DB5, partial)**: Foundation laid and
  three real ABI bugs fixed.

  - DB1 `evidencePassEvvGlobal` takes a cross-module top-names set
    (`collectTopNames`) so cross-module call sites get evv injected
    consistently.
  - DB2 `self-host/build.sh` honours `FRANKENSTEIN_EVIDENCE=plotkin`
    and forwards `--evidence=plotkin` to every host-compiler
    invocation, and `-DPLOTKIN_EVIDENCE` to `clang -c driver.c`.
  - DB3 Single-module smoke (Core/Types.hs) passes.

  Three bugs found and fixed by working back from the original
  failure mode (`Unexpected tag from consumeProgram: 1129074515`):

  1. **`self-host/driver.c` passed too few args**. The C-side
     entry-points (`consumeProgram`, compiler passes, etc.) called
     the Haskell functions with their original arity. After plotkin
     they take one extra @evv@ leading arg, so the C calls were
     undersaturated and returned a PAP closure instead of the real
     result. Fixed via `-DPLOTKIN_EVIDENCE` macro guard around
     extern decls and call sites; every C-side entry-point now
     passes `evv=0` (empty evv) as the first arg under plotkin.
  2. **`isTopLevel` missed module-qualified EVar names**. The GHC
     bridge emits `EVar (Name "Frankenstein.OrganIR.Consumer/consumeName" _)`
     for cross-module references (slash-form), but my pass only
     compared against the unqualified leaf. Result: cross-module
     calls weren't getting evv injected, so callees expecting `(evv,
     args...)` were called with `(args...)`. Fixed by splitting on
     the last @/@: if a module part is present, exact (module, leaf)
     match against `topNames`; otherwise fall back to leaf-only
     matching for backward compatibility.
  3. **`collectTopNames` swept in external-package modules**. The
     bridge pulls `OrganIR.Parse`, `Data.Map.Strict`, etc., into
     `derived` even though they're compiled by cabal (no plotkin)
     and linked from outside our pipeline. Treating them as
     plotkinable meant my pass injected evv at call sites for
     callees whose C-linked symbol still takes the original
     args. Fixed by filtering `collectTopNames` and `transformDef`
     to skip a known set of external prefixes (`OrganIR.`, `Data.`,
     `GHC.`, `System.`, `Control.`, `Text.`, `Language.`, `Type.`,
     `Numeric.`, `Foreign.`).

  After these fixes, the stage 1 plotkin self-host binary no longer
  returns wrong tags — but it now **hits the 30s build.sh
  timeout** when compiling stage 2 inputs (`Speicherzugriffsfehler`
  on timeout-induced core dump). All 24 modules still link into a
  stage 1 binary; the per-module stage 2 compilation either spins
  or runs slow enough that `timeout 30` kills it. Likely causes:
  the extra evv plumbing + `drop(evv_p)` Perceus insertions on
  every fn entry produce a perf pathology, or some interaction
  triggers an infinite loop. Needs profiling. Inline-mode bootstrap
  unaffected (still 24/24 + 21/21 + fixed point).

  DB5 (3-stage fixed point under plotkin) remains open.

## Files

| Component | File |
|---|---|
| Runtime primitives | `runtime/kk_runtime.{c,h}` |
| Runtime cycle-safety | `runtime/kk_cycle.c` |
| C-level smoke test | `runtime/test_evv_plotkin.c` |
| Compile-time pass | `src/Frankenstein/Core/EvidenceEvv.hs` |
| CLI flag wiring | `app/Main.hs` (`--evidence={inline,plotkin}`) |
| MLIR runtime decls + main wrapper | `src/Frankenstein/MlirEmit/Emitter.hs` |
| Cross-fn example | `examples/effect_ask_xfn.json` |
